<Window x:Class="to_do_list.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:to_do_list"
        mc:Ignorable="d"
        Title="To-Do List" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Your Notes" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
        <ListBox x:Name="NotesListBox" Grid.Row="1" DisplayMemberPath="Title" SelectionMode="Single"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button x:Name="CreateNoteButton" Content="Create New Note" Width="120" Margin="5"/>
            <Button x:Name="DeleteNoteButton" Content="Delete Note" Width="120" Margin="5"/>
            <Button x:Name="DuplicateNoteButton" Content="Duplicate Note" Width="120" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
