<Window x:Class="EasyToDo.Services.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="EasyToDo Update" 
        Height="200" 
        Width="400" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="True"
        Topmost="True">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="??" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBlock Text="Updating EasyToDo" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- Progress Bar -->
        <ProgressBar Grid.Row="2" 
                     Name="UpdateProgressBar" 
                     Height="20" 
                     Minimum="0" 
                     Maximum="100" 
                     Value="0"/>
        
        <!-- Status Text -->
        <TextBlock Grid.Row="4" 
                   Name="StatusText" 
                   Text="Preparing..." 
                   HorizontalAlignment="Center"
                   FontSize="12"
                   Foreground="#666"/>
        
        <!-- Info Text -->
        <TextBlock Grid.Row="5" 
                   Text="Please wait while EasyToDo updates to the latest version."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   FontSize="11"
                   Foreground="#999"
                   TextWrapping="Wrap"
                   Margin="0,10,0,0"/>
    </Grid>
</Window>